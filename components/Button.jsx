
const Button = ({id}) => {
    return (
        <>

            <button style={{display:"flex",width:"20px", height:"40px", alignItems:"center"}}>{id}</button>
        </>
    );
};

export default Button;  